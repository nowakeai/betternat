# BetterNAT v0.2.1

Status: GCP compatibility patch release. Validate downloaded artifacts with
the attached `SHA256SUMS` file before use.

## What Changed

- Hardened GCP runtime service account creation and reuse during gateway
  replacement.
- Added retry coverage for GCP IAM binding propagation errors where the runtime
  service account exists but IAM policy writes temporarily reject it as missing,
  deleted, or disabled.
- Retained provider-managed GCP runtime service accounts during gateway cleanup
  so same-name replacement does not hit GCP service-account delete/recreate
  propagation windows. BetterNAT-managed IAM bindings are still removed during
  cleanup.
- Improved agent startup state reporting so LoxiLB, route, and lease
  convergence during a fresh boot reports as `INIT` until the process has
  reached healthy `ACTIVE`. Post-ready failures still report `DEGRADED`.
- Updated GCP cleanup and IAM docs to explain retained runtime service accounts
  and manual removal after all gateways using the account are destroyed.

## Who Should Use It

Use this patch release after it is published if you run or evaluate BetterNAT on
GCP, especially with repeated gateway replacement, GKE private-node egress, or
small gateway machine types.

AWS users do not need this patch for an AWS-specific fix, but can adopt it as
the current v0.2 runtime once the release validation is complete.

## Validation

Completed before publication:

- `GOCACHE=$PWD/tmp/go-build go test ./cmd/... ./internal/... ./pkg/...`
  passed.
- `GOCACHE=$PWD/tmp/go-build go build ./cmd/betternat ./cmd/betternat-agent
  ./cmd/terraform-provider-betternat` passed.
- A disposable GKE compatibility matrix passed on 2026-06-29 in
  `smooth-calling-490406-d9`, region `us-west1`, zone `us-west1-a`, using fixed
  local artifacts:
  - `e2-small`, `e2-highcpu-2`, and `e2-standard-2` each completed two gateway
    replacement trials,
  - every trial verified the active route target and 10/10 private-node Pod
    egress probes,
  - no active-gateway `DEGRADED` window was observed,
  - no runtime service account metadata or IAM-binding failure recurred.
- After the matrix, the fixture was restored to `e2-small`; Terraform plan
  returned no changes and a post-readiness private-node Pod probe returned
  `public-ip=34.83.255.93` and `HTTP/2 200`.
- Local `v0.2.1` release-candidate artifacts were built with
  `scripts/release-build.sh` and verified with `sha256sum -c SHA256SUMS`.
- `scripts/release-dependency-scan.sh` passed with 242 modules scanned, no
  missing license files, and no restricted license keyword hits.
- A minimum GKE replacement validation passed with the versioned
  `v0.2.1` Linux amd64 artifacts hosted in the disposable validation bucket:
  - `betternat-agent_v0.2.1_linux_amd64` SHA256
    `3ff2b3cf8e207b0bae459fd56caec0008012e945c9fd7b6ec7cdfbc7bc4b8c9b`,
  - `betternat_v0.2.1_linux_amd64` SHA256
    `2b86640993e0037afe5e1d393e540495a74a8b9385eb7e9fe7b0cc1c9e9f813f`,
  - `e2-small` gateway replacement completed in 1m1s without runtime service
    account metadata or IAM-binding failure,
  - startup route and LoxiLB convergence errors were reported as `INIT`, then
    the active gateway reached continuous `ACTIVE err=""`,
  - the private-node Pod probe returned `public-ip=34.82.231.40` and
    `HTTP/2 200`,
  - Terraform `apply -refresh-only` recorded the agent-owned route target and
    the follow-up `plan -detailed-exitcode` returned no changes.

Release artifact validation:

- GitHub Release workflow run `28369282670` completed successfully and verified
  the expected asset set, including `SHA256SUMS`.
- `scripts/release-url-smoke.sh v0.2.1` passed for the default arm64 assets.
- `BETTERNAT_SMOKE_ARCH=amd64 scripts/release-url-smoke.sh v0.2.1` passed,
  executed the downloaded CLI, and reported version `v0.2.1`, commit
  `eccfac7456e9`, build date `2026-06-29T11:40:56Z`, Go `go1.25.8`, OS
  `linux`, and arch `amd64`.
- Published Linux amd64 runtime checksums:
  - `betternat-agent_v0.2.1_linux_amd64`
    `5b8ce53ad44a3a62e572fa5490e94111f9045eb07b3bf369533cd329d72f5c2b`,
  - `betternat_v0.2.1_linux_amd64`
    `49b697e12148fda419cb1324c3b66be24afd8e18acc638eaec43895e6d6bb699`.
- Published Linux arm64 runtime checksums:
  - `betternat-agent_v0.2.1_linux_arm64`
    `b46d9c08cfd23023281252bf20c880e4ac07fe7fcb4847cd82a7bd41b05cda7d`,
  - `betternat_v0.2.1_linux_arm64`
    `9d94a6ad932b0a16c887e1dfa8d55edfa5e9f0ecf4f0e77910804515e2ee413b`.
- Terraform provider built-in artifact manifest support for direct
  `betternat_version = "v0.2.1"` use is prepared in `main` from the published
  GitHub Release checksums and will be available in the next provider package
  built from that source.

## Known Limitations

- BetterNAT is self-managed and does not provide AWS NAT Gateway or Google Cloud
  NAT equivalent SLA semantics.
- Active flows may reset during failover.
- AWS gateway groups are one AZ per HA group.
- GCP gateway groups are one zone per HA group.
- Provider-managed GCP runtime service accounts are retained during gateway
  cleanup. Operators should remove them manually only after all gateways using
  the account are destroyed.
- GCP stable public identity is connectivity-first: successful new connections
  may temporarily use the target gateway's ordinary public IP before the
  regional static external IPv4 address converges back.
- Published BetterNAT AMIs are still outside this release scope.

## Artifact Integrity

Use the attached `SHA256SUMS` file from the matching GitHub Release. Terraform
Registry provider and module installs should use pinned versions.
